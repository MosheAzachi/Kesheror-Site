extends base

block content
    div.block
        h1 צור קשר
        div.col.d-flex.justify-content-center
            form(action="mailto:azachi.moshiko@gmail.com" method="post" enctype="text/plain")
                div.form-outline.mb-4
                    input(type="text", class="form-control", placeholder="שם מלא")
                div.form-outline.mb-4
                    input(type="email", class="form-control", placeholder="דואר אלקטרוני")
                div.form-outline.mb-4
                    textarea(class="form-control", id="message", rows="6", required, placeholder="הודעה")
                button(type="submit", class="btn btn-primary btn-block mb-4") שלח הודעה
        hr